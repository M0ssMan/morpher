import React, { Component } from 'react';
import { DraggableCore } from 'react-draggable';

export default class Morpher extends Component {
  render() {
    return (
      <div style={{height: 200, width: 200, backgroundColor: 'lightblue'}}></div>
    )
  }
}
